{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build firmware",
            "type": "shell",
            "command": "cargo build --bin firmware --target thumbv6m-none-eabi",
            "problemMatcher": [
                "$rustc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}